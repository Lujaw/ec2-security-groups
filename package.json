{
  "name": "ec2security",
  "version": "1.0.0",
  "description": "list the ec2 security groups",
  "main": "handler.js",
  "scripts": {
    "test": "npx jest --watch",
    "start": "sls offline start -r us-southeast-2 --noTimeout",
    "debug": "export SLS_DEBUG=* && node --inspect serverless offline -s dev"
  },
  "keywords": [
    "ec2",
    "security",
    "groups"
  ],
  "author": "Luja Shrestha",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.652.0"
  },
  "devDependencies": {
    "aws-mock": "^1.3.1",
    "aws-sdk-mock": "^5.1.0",
    "jest": "^25.2.7",
    "mock-aws": "^1.2.3",
    "serverless-offline": "^6.1.4"
  }
}
